query BlogQuery($id: Float!) {
  getOneBlog(id: $id) {
    id
    title
    description
    cover
    createdAt
    viewsCount
    body {
      id
      type
      data {
        text
        level
        file {
          ref
          url
        }
        caption
        stretched
        withBackground
        withBorder
        items
        source
        embed
        width
        height
      }
    }
    user {
      id
      username
      image
    }
  }
}
