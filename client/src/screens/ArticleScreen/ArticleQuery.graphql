query ArticleQuery($id: Float!) {
  getArticle(id: $id) {
    id
    title
    description
    cover
    readingTime
    createdAt
    tags
    category
    viewsCount
    body {
      id
      type
      data {
        text
        level
        file {
          ref
          url
        }
        caption
        stretched
        withBackground
        withBorder
        items
      }
    }
    user {
      id
      username
      image
    }
  }
}
