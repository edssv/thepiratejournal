mutation LoginMutation($loginInput: EmailLoginInput!) {
  login(loginInput: $loginInput) {
    user {
      id
      username
      image
      role
      emailVerified
    }
    accessToken
    refreshToken
  }
}
